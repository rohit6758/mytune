const fs = require('fs');

async function extractColors() {
  // Use Jimp or standard canvas to read image. Since this is a standard Node env, 
  // I will write a simple script if I can just use a library.
  // Wait, I can just use a web search or python if PIL is available.
}
